# 💕 Нашия дневник - Ръководство за настройка

Добре дошъл! Това е функционально приложение за споделяне на снимки и истории с твоята любима хора. Ето как го има да настроиш:

## 📋 Какво има вътре?

✅ **Качване на снимки** - Drag & drop или клик за качване
✅ **Галерия** - Преглед на всички снимки 
✅ **Писане на истории** - Добавяй послания и спомени с датирование
✅ **Облачно съхранение** - Firebase (автоматично запазени всички данни)
✅ **Отзивчив дизайн** - Работи на телефон, таблет, компютър

---

## 🔧 Как да настройш Firebase

Firebase е облачна услуга от Google, която е **100% безплатна** за малки приложения както това!

### Стъпка 1: Създай Firebase проект

1. Отидете на https://firebase.google.com/
2. Кликни на **"Go to console"** (в горния десен ъгъл)
3. Логвай се или създавай Google акаунт (можеш да използваш общ или личен)
4. Кликни на **"Create a new project"**
5. Въведи име (например "Нашия дневник")
6. Кликни на **Continué** и следи инструкциите
7. На последния екран, потвърди че вече е готово

### Стъпка 2: Сложи Firebase в приложението

1. След като проектът е създаден, в меню вляво кликни на **"Project Settings"** (икона на зъбче)
2. Преди до раздел **"General"** (ако не е там)
3. Скролирай надолу и намери секция **"Your apps"** или **"Веб апликация"** (может го вели със иконка на веб browser)
4. Ако няма app още, кликни на иконата на веб браузъра
5. Даць име (например "Our Diary") и кликни **"Register app"**
6. На следващия екран ще видиш кода с конфигурация - **КОПИРАЙ ЦЕЛИЯ БЛОК С firebaseConfig**

### Стъпка 3: Замени конфигурацията в приложението

1. Отвори `gallery.html` с текстов редактор
2. Намери този код (ligne 130-138):

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

3. Замени го с твоя конфигурация от Firebase

### Стъпка 4: Позволи Storage и Firestore

Сериозно, Firebaseов има **защита** - трябва да кажеш что могат да правят потребителите:

#### За снимки (Cloud Storage):
1. В меню вляво отидете на **"Storage"**
2. Кликни на **"Create"** и следи стъпките
3. Когато попита за "Location", избери място близо до теб (например "europe-west1")
4. След създанието, отидете на раздел **"Rules"**
5. Замески целия код с това:

```plaintext
rules_version = '2';
service firebase.object {
  match /b/{bucket}/o {
    match /photos/{allPaths=**} {
      allow read: if true;
      allow write: if true;
      allow delete: if true;
    }
  }
}
```

6. Кликни **"Publish"**

#### За истории (Firestore):
1. В меню вляво отидете на **"Firestore Database"**
2. Кликни на **"Create database"**
3. Избери режим: **"Start in test mode"** (това е за тестване, позволяват всички)
4. Жми далее до края

5. След създанието, отидете на раздел **"Rules"**
6. Замески целия код с това:

```plaintext
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

7. Кликни **"Publish"**

---

## 💻 Как да отворите приложението

1. На компютъра си, намери файла **`gallery.html`**
2. **Двойно кликни** на него или **дясна мишка → "Open with" → браузър**
3. Страницата трябва да се зареди в браузър (Chrome, Firefox безопасен е всички браузъри)

---

## 📱 Как да използваш приложението

### Качване на снимки:
- Кликни на областта със синиця текст или пусни снимка директно
- Максималния размер е 5MB (всички телефонни снимки са по-малки)
- Снимката ще се появи в галерия "Нашите снимки"

### Писане на история:
- Въведи заглавие (има напр. "Деня в която се срещнахме 💕")
- Напиши историята/посланието 
- Кликни **"Запази 💖"**
- История ще се появи в раздела "Нашите истории"

### Преглед и изтривание:
- Кликни на снимка за преглед
- Утра мишка над снимка/история за бутон "Изтрий"

---

## 🚀 Локален сервър (за напреднали)

Ако имаш проблеми отворяйки файла директно, може да стартираш локален сервър:

**С Python 3:**
```bash
# В папката с gallery.html:
python -m http.server 8000
```

Отваряй в браузъра: `http://localhost:8000/gallery.html`

**С Node.js:**
```bash
npm install -g http-server
http-server .
```

---

## ⚠️ Важно!

- **Защита**: Сега приложението е в "test mode" - което означава че всеки със линк може да качвам снимки. Ако искаш повече защита по-накрая, мога да добавши парола.
- **Бекап**: Firebase автоматично пази всичко. Твоите данни са сигурни!
- **Браузър**: Приложението е online-only (трябва интернет)

---

## 🎯 Следващи стъпки

Ако искаш да добавиш още функции (напр. коментари, rate за снимки, съобщения и т.н.), давай ми разведи!

Хубав дневник! 💕
